import './Header.scss';

export const Header: React.FC = () => (
  <header className="Header">
    <nav className="nav">
      <a href="/" className="logo">
        <img src="./" alt="Logo" />
      </a>
    </nav>
  </header>
);
